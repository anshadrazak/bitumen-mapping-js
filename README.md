# Bitumen.js

Bitumen.js is a lightweight JavaScript overlay mapping system
designed for asynchronous relational state propagation without
deep cloning.


const map = new BitumenMap({
  viscosity: 0.8,
  thermalBinding: true
})

map.overlay(state)
